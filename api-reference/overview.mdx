---
title: "Overview"
description: "A finer control over your Browser Sessions."
---

## Authentication

All API endpoints are authenticated using your
[Browserbase API Key](/quickstart#start-a-browser-session-from-the-code),
configured as follow:

<CodeGroup>

```bash cURL
curl -X GET "https://www.browserbase.com/v1/sessions/${SESSION_ID}" \
     -H "x-bb-api-key: ${BROWSERBASE_API_KEY}" \
```

```js Node.js/Fetch
const response = await fetch(
  `https://www.browserbase.com/v1/sessions/${sessionId}`,
  {
    method: "GET",
    headers: {
      Authorization: `x-bb-api-key: ${process.env.BROWSERBASE_API_KEY}`,
    },
  },
);
```

</CodeGroup>

## APIs

Browserbase offers 2 main APIs:

<CardGroup cols={2}>
  <Card
    title="Sessions API"
    icon="server"
    iconType="light"
    href="/api-reference/list-all-sessions"
  >
    Get full control of the Browser's Sessions.
  </Card>
  <Card
    title="Extensions API"
    icon="puzzle-piece"
    iconType="light"
    href="/api-reference/upload-an-extension"
  >
    Configure custom Chrome extensions for your Sessions.
  </Card>
</CardGroup>
